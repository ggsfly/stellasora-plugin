# 运行时配置由 Runner 根据 config_model 生成，不属插件源码（官方文档要求）
/config.toml

# 运行时数据一律写入宿主授权目录，本仓库不再维护 data/：
#   <MaiBot>/data/plugins/ggsfly.stellasora-plugin/   持久数据（dict/names/offline）
#   <MaiBot>/temp/plugins/ggsfly.stellasora-plugin/   网络缓存与直发成品缓存
# 故不再需要（也不应存在）源码内 data/ 忽略规则。

# Python
__pycache__/
*.py[cod]
.venv/

# 开发用静态检查配置（本机 venv 与 tools 导入路径，不入库）
pyrightconfig.json
# 本地 Agent/会话运行态（非插件源码）
.omo/
